<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login | Vespa Elegance</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/style.css" /> <!-- Optional external CSS -->
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f4f4f4;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .login-box {
      background: white;
      padding: 30px 40px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 350px;
    }

    .login-box h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    .login-box form input {
      width: 100%;
      padding: 10px 12px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .login-box button {
      width: 100%;
      padding: 10px;
      background-color: #1e90ff;
      color: white;
      border: none;
      border-radius: 5px;
      font-weight: bold;
      cursor: pointer;
    }

    .google-btn {
      margin-top: 15px;
      background-color: #db4437;
    }

    .login-box p {
      text-align: center;
      margin-top: 15px;
    }

    .login-box a {
      color: #1e90ff;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>Login</h2>
    
    <form action="/user/login" method="POST">
      <input type="email" name="email" placeholder="Email" required />
      <input type="password" name="password" placeholder="Password" required />
      <%if( message  && message.length>0){%>
      <div class="alert alert-danger text-center">
        <%=message%>
      </div>
      <%}%>
      <button type="submit">Login</button>
    </form>
<br>

<a href="/auth/google"
                  class="btn btn-outline-dark w-100 d-flex align-items-center justify-content-center"
                  style="background-color: white; color: black; border: 1px solid black; font-weight: 500;"
                  onmouseover="this.style.backgroundColor='#0d6efd'; this.style.color='white'; this.style.borderColor='#0d6efd';"
                  onmouseout="this.style.backgroundColor='white'; this.style.color='black'; this.style.borderColor='black';">
                  <img src="https://developers.google.com/identity/images/g-logo.png" alt="Google" width="20"
                    height="20" class="me-2">
                  Sign Up with Google
                </a>


      <!--  <a href="/auth/google" class="btn btn-outline-danger w-100">
  <img src="img/googlesignup.png" alt="Google Logo" style="width: 24px; height: 24px; margin-right: 8px;" />
  Sign in with Google
</a>


      <form action="/auth/google" method="GET">
      <button class="google-btn" type="submit">Sign in with Google</button>
    </form>   -->

    <p>Don't have an account? <a href="/signUp">Sign up</a></p>
    <div class="text-center mt-2">
    <p><a href="/forgotPassword" class="sign-up-link">Forgot password?</a></p>
    </div>
  </div>  
  <!-- SweetAlert -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<% if (typeof success !== 'undefined' && success.length > 0) { %>
<script>
  Swal.fire({
    icon: "success",
    title: "<%= success %>",
    showConfirmButton: false,
    timer:2000
  })
</script>
<% } %>

</body>
</html>
